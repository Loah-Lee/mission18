services:
  - type: web
    name: mission18-backend
    runtime: python
    rootDir: 1팀_이소윤/backend
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn app.main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: PYTHON_VERSION
        value: 3.10.14
